@model WebParqueo.Models.Ajustes

@{
    ViewBag.Title = "Eliminar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row" style="margin-top:30px">
    <div class="col-sm-12">

        <div class="panel panel-warning">
            <div class="panel-heading">
                <h4>Eliminar Registro</h4>
            </div>
            <div class="panel-body">
                <div class="alert alert-danger">
                    ¿Desea eliminar el Registro:
                    @Html.DisplayTextFor(m => m.ID_Ajustes) "-----"
                    Nombre.:
                    @Html.DisplayTextFor(m => m.NombreParqueo)"-----"
                    Apellidos.:
                    @Html.DisplayTextFor(m => m.CantidadParqueo)"-----"
                    Teléfono.:
                    ?
                </div>
                <form action="@Url.Action("Eliminar","Ajustes")" method="post">
                    <input type="hidden" class="form-control" name="ID_Ajustes" value="@Html.DisplayTextFor(m => m.ID_Ajustes)" />

                    <button type="submit" class="btn btn-danger">Eliminar</button>
                    <a class="btn btn-warning" href="@Url.Action("Inicio","Ajustes")">Volver</a>
                </form>

            </div>
        </div>


    </div>
</div>